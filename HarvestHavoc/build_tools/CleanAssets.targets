<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
    <Target Name="DeleteAssets" BeforeTargets="Clean">
        <Message Text="Deleting asset files..." Importance="high" />
        <RemoveDir Directories="$(SolutionDir)bin\$(Platform)\$(Configuration)\assets" />
    </Target>
</Project>
